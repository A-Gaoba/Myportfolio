import React from 'react';
import ProjectsList from './ProjectsList';

const ProjectsSection: React.FC = () => (
  <section className="p-16 md:w-[90%] xl:w-[70%] w-full">
    <ProjectsList />
  </section>
);

export default ProjectsSection;
